# 桌面整理助手 · 技术方案

> 项目代号：Cubby
> 仓库：https://github.com/1EM0NS/Cubby （Public）
> 目标平台：Windows 10 21H2+ / Windows 11（含 24H2）
> 技术栈：.NET 8 + WPF + Win32 互操作（x64）

---

## 1. 项目定位与差异化

### 1.1 要解决的问题

现有方案（腾讯桌面整理 DeskGo、360 桌面助手、小智桌面）都是**接管桌面输入**的思路：铺一层透明窗口在上层、自己拦截鼠标消息。这带来一个无法回避的副作用——

**开启整理后，Wallpaper Engine 等动态壁纸的交互元素（壁纸右上角按钮、网页壁纸可点区域）全部点不动。**

原因不是资源占用，而是输入抢占：桌面图标层 `SysListView32` 是全屏透明窗口、会消费鼠标事件，Wallpaper Engine 靠**全局鼠标钩子（`SetWindowsHookEx` + `WH_MOUSE_LL`）**拦截并转发才能实现互动壁纸；桌面整理类软件同样要接管双击/右键/拖拽，一旦它的钩子抢先返回非零值把消息吞掉，钩子链后面的 WE 就永远收不到点击。官方只能提供"兼容第三方壁纸"开关，并承认"交互型壁纸暂不支持"。

### 1.2 我们的核心设计原则（差异化护城河）

> **只借用盒子区域内的点击，其余鼠标消息原封不动下发。**

四条硬约束，任何功能设计都不得违反：

| 编号 | 约束 | 理由 |
|---|---|---|
| P1 | **不注册全局鼠标钩子**。若某功能确实必须钩子，钩子函数必须无条件 `CallNextHookEx` 放行，禁止返回非零值 | 不破坏 WE 等任何依赖钩子链的软件 |
| P2 | 浮层使用**逐像素 alpha 的分层窗口**或 **`SetWindowRgn` 区域窗口**，只有盒子区域参与命中测试 | alpha=0 的像素系统自动放行点击，点击直接落到图标层/WE |
| P3 | **不 `SetParent` 到 `WorkerW`**，使用独立顶层窗口 + 常驻置底 | `SetParent` 后鼠标事件经常失效，且会与 WE 争抢 Z 序导致壁纸闪烁 |
| P4 | **文件实体永不移动**，只保存"引用 + 展示位置" | 最坏情况只丢配置，不会丢用户文件 |

其余非目标（明确不做）：修改 Explorer、云同步、AI 文件解析、常驻后台服务、强制的右键菜单集成。

---

## 2. Windows 桌面图层模型（实现基础）

```
Z 序自上而下：
  应用窗口层        普通程序窗口，压在我们的浮层之上
  本程序整理浮层    只有盒子区域参与命中测试，其余区域点击穿透
  图标层            Progman / SHELLDLL_DefView / SysListView32
                    全屏透明，桌面图标的双击/右键原本在此处理
  壁纸层            WorkerW（由 Progman 响应未公开消息 0x052C 派生）
                    Wallpaper Engine 把播放窗口 SetParent 到这一层
  Progman           桌面根窗口
```

点击归属：

- 落在盒子区域内 → 本程序处理（双击打开、右键菜单、拖拽）
- 落在盒子外 → **原样穿透到图标层 / 壁纸层**，与未安装本程序时行为完全一致
- 不需要任何钩子、不需要任何消息拦截

### 2.1 为什么不能用 `WM_NCHITTEST → HTTRANSPARENT`

该返回值只在**同一线程的窗口链**上继续向下传递；跨进程（我们的窗口 → Explorer 的 `SysListView32`）时消息会被丢弃而不是转发。因此它不能用来实现"穿透到桌面"。

### 2.2 可用的两种穿透机制（二选一或组合）

| 机制 | 原理 | 优点 | 缺点 |
|---|---|---|---|
| **逐像素 alpha（首选）** | `WS_EX_LAYERED` + `UpdateLayeredWindow`，alpha=0 的像素不参与命中测试，鼠标消息直接落到下层窗口 | 天然支持圆角/阴影/半透明，穿透粒度是像素级 | 整屏位图更新有成本（4K 约 33MB/帧），需脏矩形与缓存 |
| **`SetWindowRgn`（兜底）** | 窗口区域 = 所有盒子矩形的并集，区域外既不绘制也不命中 | 实现简单、性能好、跨进程可靠 | 区域是 1 位掩码，圆角会锯齿；区域变更会闪烁 |

**决策**：主用逐像素 alpha；若阶段一实测发现 WPF 的 `AllowsTransparency` 窗口在 alpha=0 处不放行点击，则降级为 `SetWindowRgn(盒子并集, CreateRoundRectRgn)`，视觉损失可接受。

### 2.3 置底策略

- 窗口样式：`WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW | WS_EX_LAYERED`，**不设置 `WS_EX_TOPMOST`**
- 不抢焦点：点击盒子不应激活我们的窗口（避免任务栏闪动、避免抢走 WE 焦点）
- 维持 Z 序：监听 `EVENT_SYSTEM_FOREGROUND` / `EVENT_SYSTEM_MINIMIZESTART` / `EVENT_SYSTEM_MINIMIZEEND`（`SetWinEventHook`）以及自身 `WM_WINDOWPOSCHANGING`，事件驱动地把窗口重新放到应用窗口之下
- **待验证风险**：直接 `SetWindowPos(HWND_BOTTOM)` 有被压到 `Progman` 之下（不可见）的风险。阶段一必须验证"可见 + 可点 + 不遮挡应用窗口"三者同时成立，备选方案是相对 `SysListView32` 所在层级做插入式 Z 序调整
- `Win+D`（显示桌面）会连带隐藏我们的顶层窗口，需要在 `EVENT_SYSTEM_FOREGROUND`/shell 通知后恢复显示

---

## 3. 工程结构

```
Cubby/
├─ Cubby.sln
├─ 技术方案.md                     # 本文档
├─ .gitignore / .gitattributes
├─ .github/
│  ├─ workflows/ci.yml             # CI：设计原则守卫 + 构建 + 测试
│  ├─ ISSUE_TEMPLATE/task.yml      # Issue 模板（验收标准必填）
│  └─ pull_request_template.md     # PR 模板（验证说明必填）
├─ docs/
│  └─ adr/                         # 架构决策记录
├─ src/
│  ├─ Cubby.App/                # WPF 主程序：托盘、设置、盒子渲染与交互
│  │  ├─ Views/                    # 盒子控件、设置面板、右键菜单
│  │  ├─ ViewModels/
│  │  └─ Tray/
│  ├─ Cubby.Core/               # 领域层：盒子/条目模型、布局引擎、归类规则、持久化
│  │  ├─ Model/
│  │  ├─ Layout/
│  │  ├─ Rules/
│  │  └─ Storage/
│  ├─ Cubby.Shell/              # Win32 互操作层：分层窗口、Z 序、多屏/DPI、图标层只读、拖放
│  │  ├─ Windows/
│  │  ├─ Input/
│  │  ├─ Monitors/
│  │  └─ Desktop/
│  └─ Cubby.Tests/              # 单元测试 + 集成测试
└─ tools/
   ├─ scripts/guard.ps1             # 设计原则守卫（CI 与本地共用）
   ├─ ZOrderProbe/                 # 窗口链 / Z 序快照工具（验收用）
   └─ HookProbe/                   # 鼠标钩子链探针：验证我们的进程没有吞消息（验收用）
```

分层依赖方向：`App → Core ← Shell`，`Core` 不直接引用任何 Win32 类型（模型与状态可单元测试），`Shell` 只暴露接口与 DTO。

### 3.1 关键接口（Core 定义，Shell 实现）

```csharp
public interface IOverlayWindow : IDisposable
{
    nint Handle { get; }
    void SetHitRegions(IReadOnlyList<Rect> regions);   // 盒子并集，用于命中测试
    void Invalidate(Rect dirty);                        // 脏矩形重绘
    void EnsureBehind();                                // 重新置底
    event EventHandler<MonitorChangedEventArgs> MonitorChanged;
}

public interface IDesktopIconProbe            // 只读，永不写入
{
    IReadOnlyList<DesktopIconInfo> ReadIcons(int screenX, int screenY); // 名称 + 位置
    bool IsSupported { get; }                  // 探测失败时整体降级
}
```

---

## 4. Win32 API 清单与风险等级

| API / 消息 | 用途 | 风险 | 备注 |
|---|---|---|---|
| `SetWindowLongPtr`(GWL_EXSTYLE) | 设置 `WS_EX_LAYERED`/`NOACTIVATE`/`TOOLWINDOW` | 低 | 基础能力 |
| `UpdateLayeredWindow` | 逐像素 alpha 提交位图 | 中 | 需自己管理位图缓存与脏矩形 |
| `SetWindowRgn` / `CreateRoundRectRgn` | 兜底的命中区域方案 | 低 | 区域变更会闪烁 |
| `SetWindowPos` | 置底 / Z 序调整 | 中 | 需验证不会被压到 Progman 之下 |
| `SetWinEventHook` | 监听前台窗口/最小化，事件驱动置底 | 低 | 只监听，不拦截 |
| `EnumDisplayMonitors` / `GetMonitorInfo` | 多屏枚举与虚拟桌面坐标 | 低 | |
| `SetProcessDpiAwarenessContext`(PMv2) | 每屏 DPI 感知 | 低 | 通过 app.manifest 声明 |
| `FindWindow("Progman")` / `SHELLDLL_DefView` | 定位桌面图标层 | 中 | Win11 24H2 层级有变化，需版本适配 |
| `LVM_GETITEMCOUNT` / `LVM_GETITEMPOSITION` / `LVM_GETITEMTEXT` | 读取桌面图标名称与位置（吸附） | **高** | 跨进程需 `VirtualAllocEx` + `WriteProcessMemory` 分配缓冲；**只读，禁止写位置** |
| `SysListView32` + `ShowWindow` | 隐藏/显示真实桌面图标 | **高** | Win11 行为差异；隐藏后需实测 WE 互动是否仍正常 |
| `RegisterDragDrop` / `IDropTarget` | 接收从桌面拖入的文件 | 低 | 标准 OLE 拖放，跨进程可靠 |
| `DoDragDrop` + `CF_HDROP` | 从盒子拖出到其他程序 | 低 | |
| `SetWindowsHookEx`(WH_MOUSE_LL) | **默认不引入** | **禁止** | 仅当双击空白桌面隐藏图标等实验功能需要时，必须无条件 `CallNextHookEx` |

**位宽要求**：与 Explorer 做 `LVM_*` 通信要求同位数，进程必须固定为 **x64**（不要用 AnyCPU 跑成 x86）。

---

## 5. 数据结构与持久化

### 5.1 模型

```csharp
public sealed record BoxItem
{
    public string Id { get; init; }
    public string DisplayName { get; init; }
    public string TargetPath { get; init; }        // 引用，绝不移动实体
    public ItemKind Kind { get; init; }            // File / Folder / Url / Mapped
    public double X { get; init; }                 // 盒子内相对坐标
    public double Y { get; init; }
    public bool IsPinned { get; init; }
}

public sealed record Box
{
    public string Id { get; init; }
    public string Name { get; init; }
    public string MonitorId { get; init; }         // 绑定显示器（设备名 + 序号）
    public Rect Bounds { get; init; }              // DIP 坐标，便于 DPI 换算
    public int Columns { get; init; }
    public double Opacity { get; init; }
    public bool IsLocked { get; init; }
    public bool IsCollapsed { get; init; }
    public string? MappedFolder { get; init; }     // 一期：文件夹映射
    public List<BoxItem> Items { get; init; }
}

public sealed record LayoutDocument
{
    public int SchemaVersion { get; init; } = 1;
    public List<Box> Boxes { get; init; }
    public Dictionary<string, MonitorLayout> Monitors { get; init; }
}
```

### 5.2 存储策略

- 布局：`%AppData%\Cubby\layout.json`，**原子写**（写临时文件 → `File.Replace`）
- 布局快照备份：`%AppData%\Cubby\snapshots\{时间戳}.json`，一期做多版本保留
- 索引/日志：轻量场景用 JSON 即可；条目数超过约 5k 再引入 SQLite（`Microsoft.Data.Sqlite`）
- `SchemaVersion` 必须存在，升级时走迁移函数，禁止直接反序列化未知版本
- 显示器标识用 `设备名 + 分辨率` 组合并保留"最近匹配"回退，避免拔插显示器后盒子跑到主屏（这是腾讯的已知缺陷）

---

## 6. 功能清单与实现要点

### 6.1 MVP

| 功能 | 实现要点 |
|---|---|
| 浮动盒子（新建/拖拽/缩放/重命名/锁定/折叠） | WPF 自绘；拖拽与缩放由浮层窗口统一处理，不使用系统非客户区 |
| 拖入文件生成引用 | `IDropTarget` 接收 `CF_HDROP`；只记录路径，不移动文件 |
| 双击打开 / 右键菜单 / 拖出还原 | `ShellExecuteEx`；拖出用 `DoDragDrop` + `CF_HDROP`；右键菜单用自绘 WPF 菜单或 `ShellContextMenu` |
| 从桌面吸附图标 | 只读 `LVM_GETITEMPOSITION` + 文件系统枚举交叉匹配；失败整体降级为"不吸附"，**绝不写位置** |
| 布局持久化 | 见 5.2，原子写 + 版本号 |
| 多屏与 DPI 自适应 | 每显示器一个浮层窗口；响应 `WM_DISPLAYCHANGE` / `WM_DPICHANGED`；坐标统一存 DIP |
| 托盘 + 开机自启 + 样式设置 | 最小化到托盘；自启用注册表 `Run` 或任务计划；样式含透明度、圆角、列数、字号 |

### 6.2 一期

| 功能 | 实现要点 | 主要风险 |
|---|---|---|
| 隐藏/显示真实桌面图标 | 对 `SysListView32` 做 `ShowWindow(SW_HIDE/SW_SHOW)`；提供**托盘菜单 / 全局热键 / 盒子按钮**三种入口 | 高级 |
| （可选实验）双击空白桌面切换隐藏 | 需要低级别鼠标钩子；必须严格 `CallNextHookEx` 放行，且默认关闭、开关可关 | 高级，与 P1 冲突，需专项回归 |
| 盒子内即时搜索 | 内存索引 + `FileSystemWatcher`；`InternalBufferSize` 调大并处理缓冲区溢出重建索引 | 中 |
| 自动归类规则 | JSON 规则 DSL（扩展名 / MIME / 时间 / 来源路径 / 正则），规则可预览、可撤销 | 低 |
| 文件夹映射 | 把一个磁盘文件夹的内容映射为盒子条目，实体不动（缓解 C 盘） | 中 |
| 布局快照备份与还原 | 快照列表 + 一键还原；分辨率变化后自动重排并在还原时提示 | 中 |

---

## 7. 关键流程

### 7.1 启动

1. 声明 `PMv2` DPI 感知 → 枚举显示器 → 读取 `layout.json` 并做显示器匹配回退
2. 为每个显示器创建浮层窗口（分层窗口 / 区域窗口），服务启动即调用 `EnsureBehind()`
3. 注册 `SetWinEventHook` 监听前台变化；注册托盘图标
4. 探测 `SysListView32` 可访问性，失败则关闭"吸附"与"隐藏图标"两项能力并提示

### 7.2 一次点击的完整路径

1. 用户在桌面空白处点击 → 命中测试判定该像素 alpha=0（或不在区域内）→ 系统把消息送给下层窗口 → 图标层/WE 正常响应，**我们完全无感**
2. 用户点击盒子里某个图标 → 命中测试落在盒子区域 → 我们收到消息 → 按条目类型执行（打开/右键/拖拽）
3. 全流程没有任何钩子、没有任何消息转发

---

## 8. 性能预算

| 指标 | 目标 | 说明 |
|---|---|---|
| 空闲 CPU | ≈ 0%（无定时轮询，全部事件驱动） | 禁止轮询式 `SetWindowPos` 置底 |
| 常驻内存 | ≤ 120MB | WPF 基线现实成本；作为取舍点接受，不追求腾讯的 30–50MB |
| GDI / 用户对象 | 长时间运行无增长 | `GetGuiResources` 定期采样 |
| 布局变更响应 | 感知无延迟 | 脏矩形重绘，禁止全屏频繁提交 |
| 鼠标移动开销 | 零额外分配 | 不在 `WM_MOUSEMOVE` 路径做分配或布局计算 |

---

## 9. 测试与验收

### 9.1 核心验收标准（可自动化）

| 编号 | 验收项 | 方法 |
|---|---|---|
| A1 | 开启本程序前后，**WE 互动壁纸点击行为完全一致** | `tools/HookProbe` 记录钩子链中消息到达情况 + 模拟鼠标点击后断言 WE 侧响应 |
| A2 | 盒子外点击不改变桌面原有行为（图标单击/框选/右键菜单） | UI 自动化回归脚本 |
| A3 | 本程序不注册任何吞消息的钩子 | 静态检查 + HookProbe 断言 |
| A4 | Z 序正确：始终在应用窗口之下、图标层之上 | `tools/ZOrderProbe` 输出窗口链快照并做断言 |
| A5 | 多屏 / DPI / 分辨率变化后盒子位置正确 | 分辨率切换 + `WM_DISPLAYCHANGE` 注入 + 拔插显示器矩阵 |
| A6 | 显示桌面（Win+D）后浮层恢复 | 键盘注入 + 断言可见 |
| A7 | 长时间运行无内存/句柄/GDI 泄漏 | 挂机采样 `GetGuiResources` 与工作集 |
| A8 | 与冲突软件共存 | Wallpaper Engine / Fences / 腾讯桌面整理 / 360 桌面助手 并存场景 |

### 9.2 测试工具（自研，放 `tools/`）

- **ZOrderProbe**：枚举窗口链，输出 `Progman → WorkerW → SysListView32 → 我们的浮层 → 应用窗口` 的顺序快照，支持前后对比
- **HookProbe**：安装 `WH_MOUSE_LL`，记录每次消息是否到达后续钩子，用于证明"我们没吞消息"（这是 A1/A3 的客观证据，避免只靠人眼点壁纸）
- **LayoutProbe**：模拟 `WM_DISPLAYCHANGE`、DPI 切换、显示器增删

### 9.3 回归清单（每次改动必跑）

1. 桌面空白处单击/框选/右键
2. 桌面图标双击、拖动位置
3. WE 互动壁纸按钮点击（A1）
4. 盒子内双击、右键、拖出到资源管理器
5. 桌面拖文件进盒子
6. 分辨率切换 / 主副屏交换 / DPI 125%↔150%
7. Win+D、Win+M、任务视图
8. 全屏游戏/全屏视频下浮层不干扰

---

## 10. 风险清单与降级策略

| 风险 | 影响 | 降级策略 |
|---|---|---|
| WPF `AllowsTransparency` 窗口在 alpha=0 处不放行点击 | 核心机制失效 | 阶段一先验证；降级 `SetWindowRgn` 区域方案 |
| `HWND_BOTTOM` 置底把窗口压到 Progman 之下 | 浮层不可见 | 改为相对图标层级插入式 Z 序；必要时把浮层作为 `DefView` 的兄弟层处理（仍不使用 `SetParent`） |
| Win11 24H2 桌面层级变化 | 定位 `SysListView32` 失败 | 按系统版本分支适配；失败即整体关闭图标相关能力，浮层功能不受影响 |
| `Win+D` 后浮层不恢复 | 用户以为程序挂了 | 监听 shell 事件强制恢复；`WM_WINDOWPOSCHANGING` 中纠正 |
| 逐像素位图更新开销 | 拖拽时卡顿 | 脏矩形 + 位图缓存 + 拖拽期间降低刷新频率 |
| 隐藏真实图标后 WE 互动异常 | 破坏 P1 目标 | 提供开关并默认关闭，A1 用例覆盖 |
| 与腾讯桌面整理等同时安装 | 互相抢桌面 | 启动检测已知进程并提示用户二选一，不做抢占 |

---

## 11. 里程碑与验收门禁

不设时间点，按门禁推进，**每阶段必须通过门禁才进入下一阶段**。

| 阶段 | 交付物 | 门禁（必须全部通过） |
|---|---|---|
| **M0 机制验证（Spike）** | 单个显示器上的空白浮层窗口 | 浮层可见；盒子区域可接收点击；盒子外点击穿透到图标层；WE 互动壁纸按钮 A1 用例与基线一致；A4 Z 序正确 |
| **M1 骨架可用** | 多屏浮层 + 盒子渲染 + 拖入拖出 + 双击打开 + 持久化 + 托盘 | A1–A7 全通过；内存/GDI 基线建立 |
| **M2 一期功能** | 隐藏图标、盒内搜索、归类规则、文件夹映射、布局快照 | 新功能各自的专项用例 + 9.3 回归清单全绿；A8 共存场景通过 |
| **M3 发布就绪** | 安装包、签名、首次运行引导、崩溃日志 | 稳定性挂机无泄漏；安装/卸载无残留（含桌面图标与自启项还原） |

---

## 12. 已定与待定事项

已定：

1. 项目名称 **Cubby**，仓库 https://github.com/1EM0NS/Cubby （Public）
2. 技术栈 .NET 8 + WPF，进程固定 x64（与 Explorer 同位宽才能做 `LVM_*` 通信）
3. 开发流程：trunk-based + PR，Conventional Commits，SemVer 与里程碑绑定（见第 13 章）
4. "双击空白桌面隐藏图标"默认不做（与 P1 冲突），改为托盘菜单 / 全局热键 / 盒子按钮三种入口

待定（不阻塞 M0）：

1. 是否正式支持 Windows 10（影响 `SysListView32` 与 DPI 分支）
2. 安装包形式：Inno Setup / MSIX（MSIX 需签名证书）
3. 是否做皮肤系统（影响盒子渲染层抽象成本）

---

## 13. 开发流程与版本迭代

### 13.1 分支模型（trunk-based）

`main` 永远可构建、可运行，**不直推**，一律走 PR。单人开发也走 PR，目的是留下"当时为什么这么做"的记录，尤其是 M0 阶段试错过程的结论。

| 分支前缀 | 用途 |
|---|---|
| `spike/` | 机制验证，允许粗糙、允许失败，结论文档化后即可关闭 |
| `feat/` | 新功能 |
| `fix/` | 缺陷修复 |
| `test/` | 测试与验收工具 |
| `ci/` `docs/` `chore/` | CI、文档、工程杂项 |

### 13.2 提交规范（Conventional Commits）

格式 `type(scope): 简述`；type 与 scope 用英文以保证工具链可用，正文可用中文。

```
feat(overlay): 实现盒子区域命中测试与点击穿透
fix(zorder): 修复 Win+D 后浮层不再恢复显示
test(probe): 增加 HookProbe 钩子链断言
```

type 取值：`feat` `fix` `refactor` `perf` `test` `docs` `chore` `ci`。
版本号推导：`feat` → minor，`fix` / `perf` → patch，`BREAKING CHANGE` → major。

### 13.3 版本号与里程碑映射

| 版本 | 对应门禁 |
|---|---|
| `0.1.0` | M0 机制验证通过 |
| `0.2.0` | M1 骨架可用 |
| `0.3.0` | M2 一期功能 |
| `1.0.0` | M3 发布就绪 |

平时提交不带版本号；**只在跨过门禁时打 tag 并发布 Release**，Release 说明必须附该门禁的验收结论（引用 A1–A8 编号）。

### 13.4 单个功能的闭环

1. 建 Issue（用 `task.yml` 模板，验收标准必须引用 A1–A8 编号）
2. 切分支 → 实现 → 本地跑 9.3 回归清单
3. 提 PR（描述写清"改了什么 / 怎么验证的 / 验证结果"）
4. CI 在 `windows-latest` 上跑：设计原则守卫 → 构建 → 测试
5. 自查通过后合并，删除分支并关闭 Issue
6. 跨过门禁 → 打 tag → 发 Release

### 13.5 设计原则守卫（CI 门禁）

P1–P4 不只是文档约定，由 CI 强制执行：`.github/workflows/ci.yml` 扫描 `.cs` 源码，出现下列调用即构建失败。

| 被拦截的符号 | 违反的原则 |
|---|---|
| `SetWindowsHookEx` | P1 不注册全局鼠标钩子 |
| `SetParent` | P3 不挂载到 WorkerW |
| `WS_EX_TRANSPARENT` | P2 整窗穿透会让盒子无法交互 |
| `LVM_SETITEMPOSITION` / `LVM_SETITEMPOSITION32` / `LVM_ARRANGE` | P4 不写桌面图标位置 |

确有必要时，在调用行前两行内加入豁免注释，说明原因：

```csharp
// guard-exempt: 仅用于读取图标位置做吸附，绝不写回
var position = ReadItemPosition(index);
```

### 13.6 架构决策记录（ADR）

`docs/adr/` 下每篇记录一个反直觉或不可逆的关键决策，格式为 背景 / 决策 / 理由 / 后果。凡是会触碰 P1–P4 的改动，**先有 ADR 再有代码**。

---

## 附录 A：核心代码骨架（伪代码，仅示意）

```csharp
// 浮层窗口创建（细节待 M0 实测确定）
var exStyle = WS_EX_LAYERED | WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW;
// 不设置 WS_EX_TRANSPARENT —— 盒子内需要接收点击

// 命中区域（兜底方案）
var rgn = CreateRectRgn(0, 0, 0, 0);
foreach (var box in boxes)
{
    var boxRgn = CreateRoundRectRgn(box.Left, box.Top, box.Right, box.Bottom, radius, radius);
    CombineRgn(rgn, rgn, boxRgn, RGN_OR);
    DeleteObject(boxRgn);
}
SetWindowRgn(hwnd, rgn, true);

// 事件驱动置底（绝不轮询）
SetWinEventHook(EVENT_SYSTEM_FOREGROUND, EVENT_SYSTEM_FOREGROUND,
                nint.Zero, callback, 0, 0, WINEVENT_OUTOFCONTEXT);
// callback 中调用 EnsureBehind() → SetWindowPos(...)
```